# Samsung Rennie Tonna 2025

**Source:** Local file upload

## Local file upload

- `Samsung Rennie Tonna Results .xlsx` — local file moved here from `_DATA_/_unsorted/` after the
  reorganization confirmed its year. File mtime: 2025-10-24 22:46 UTC.

The parser dispatcher in `scripts/phase0/cli.py` matches on filename substring
after lowercasing, so this file can be loaded directly with `cli.py load --file <path>`.
